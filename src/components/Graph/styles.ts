import styled from 'styled-components';

const WIDTH = 1000;
const HEIGHT = 800;

export const Container = styled.div`
  border: 1px solid black;
  height: ${HEIGHT}px;
  margin: 0 auto;
  position: relative;
  width:  ${WIDTH}px;
`;
