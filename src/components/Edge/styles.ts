import styled from 'styled-components';

export const Line = styled.div`
  background: linear-gradient(to top right, rgba(0,0,0,0) calc(50% - 2px), green, rgba(0,0,0,0) calc(50% + 2px) );
  position: absolute;
`;
