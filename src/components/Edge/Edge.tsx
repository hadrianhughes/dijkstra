import React from 'react';
import { Atom } from '../../types';

interface PropTypes {
  from: Atom;
  to:   Atom;
}

const Edge = ({ from, to }: PropTypes) => {
  return <div></div>;
};

export default Edge;
